00. you are building the language-agnostic clean-room specifications for an omnifocus clone

0a. familiarize yourself with @specs/

1. read @SPECIFICATION_PLAN.md and use websearch/webfetch to research the single highest priority item and add it to existing information in specs/

2. update SPECIFICATION_PLAN.md with your progress

3. use `git add file1 file2 ...` and `git commit -m "..."` to commit your changes - do not include any claude attribution

4. use `git push origin main` to push your commits
